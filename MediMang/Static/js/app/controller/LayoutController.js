angular.module('app').controller('LayoutController', [
         '$scope',
         '$window',
         'application',
         "blockUI",
    function ($scope, $window, application, blockUI) {
        // get message message
    }
]);